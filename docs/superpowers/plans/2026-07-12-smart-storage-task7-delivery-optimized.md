# Smart Storage Delivery Optimized Plan From Task 7

> This file is a new optimized continuation plan. It does not replace the original plan at `docs/superpowers/plans/2026-07-10-smart-storage-delivery.md`.

**Goal:** Keep the original final goal unchanged: finish the HarmonyOS "智能收纳管家" app as a usable competition-ready app with item, space, event, profile, immersive UI, sharing, service card, and submission materials.

**Current state:** Tasks 1-6 are already implemented in the current worktree. The remaining plan should not restart core feature work. It should polish the current app, add platform features with controlled risk, and prepare competition materials.

**Architecture:** Keep the existing HarmonyOS 6.1 ArkTS single-entry app. Preserve the current three-layer shape: pages under `entry/src/main/ets/pages`, reusable UI under `entry/src/main/ets/components`, models under `entry/src/main/ets/model`, and RelationalStore repositories under `entry/src/main/ets/data`. Use ArkUI native blur/navigation as the default. HDS is optional only if locally proven available and build-safe.

**Tech Stack:** ArkTS / ArkUI / HarmonyOS 6.1 API 24 / RelationalStore / DevEco Studio or deveco-code.

## Global Constraints

- Work only in worktree `C:\Users\xushe\Desktop\666\.worktrees\codex_plus_deveco`.
- Branch is `codex_plus_deveco`.
- Target SDK stays `6.1.1(24)`.
- Do not introduce third-party dependencies.
- Keep strict ArkTS compatibility with `caseSensitiveCheck: true`.
- Register every routable page in `entry/src/main/resources/base/profile/main_pages.json`.
- Use ArkUI native blur/navigation unless `@kit.UIDesignKit` or HDS APIs are already available and confirmed by build.
- After every task, run DevEco build or the available `deveco-code` build command and commit only source/config/docs changes.
- Do not commit `.hvigor/`, `.idea/`, `oh_modules/`, `build/`, or local generated output.
- Prefer small, focused diffs. Do not refactor repository/data behavior during visual polish unless fixing a build or runtime bug.

---

## Confirmed Baseline After Task 6

Already present:

- `entry/src/main/ets/pages/Index.ets`: native ArkUI top tabs plus bottom navigation.
- `entry/src/main/ets/pages/ItemPage.ets`: item list/search/category filter/add navigation.
- `entry/src/main/ets/pages/SpacePage.ets`: three-level room/cabinet/grid drill-down.
- `entry/src/main/ets/pages/EventPage.ets`: active/completed event list.
- `entry/src/main/ets/pages/ProfilePage.ets`: dashboard with item/category/event stats and reminders.
- `entry/src/main/ets/pages/AddItemPage.ets`
- `entry/src/main/ets/pages/ItemDetailPage.ets`
- `entry/src/main/ets/pages/CreateEventPage.ets`
- `entry/src/main/ets/pages/EventDetailPage.ets`
- `entry/src/main/ets/components/EmptyState.ets`
- `entry/src/main/ets/components/ItemCard.ets`
- `entry/src/main/ets/components/CategoryGrid.ets`
- `entry/src/main/ets/components/EventCard.ets`
- `entry/src/main/ets/data/SpaceRepository.ets`
- `entry/src/main/ets/data/ItemRepository.ets`
- `entry/src/main/ets/data/EventRepository.ets`
- `entry/src/main/resources/base/profile/main_pages.json`: registered core routable pages.

Observed constraints:

- `oh-package.json5` and `entry/oh-package.json5` do not declare HDS dependencies.
- Current route/navigation code uses `@kit.ArkUI`.
- Service card files are not present yet.
- Event share code is not present yet.
- `entry/src/main/module.json5` currently has `EntryAbility` and `EntryBackupAbility`.

---

## Remaining File Map

Likely modify:

- `entry/src/main/ets/pages/Index.ets`
- `entry/src/main/ets/pages/ItemPage.ets`
- `entry/src/main/ets/pages/SpacePage.ets`
- `entry/src/main/ets/pages/EventPage.ets`
- `entry/src/main/ets/pages/ProfilePage.ets`
- `entry/src/main/ets/pages/AddItemPage.ets`
- `entry/src/main/ets/pages/ItemDetailPage.ets`
- `entry/src/main/ets/pages/CreateEventPage.ets`
- `entry/src/main/ets/pages/EventDetailPage.ets`
- `entry/src/main/ets/components/*.ets`
- `entry/src/main/module.json5`

Likely create for service card only:

- `entry/src/main/ets/form/EntryFormAbility.ets` or the path generated by DevEco's API 24 form template.
- form resource/profile files required by the local DevEco template.

Likely create for docs:

- `docs/competition/architecture.md`
- `docs/competition/test-report.md`
- `docs/competition/screenshots.md`

---

## Task 7: ArkUI Immersive Polish Pass

**Intent:** Polish the already implemented app without changing its data model or completed flows.

**Files:**

- Modify: `entry/src/main/ets/pages/Index.ets`
- Optional Modify: current main/detail pages and shared components as needed.

**Interfaces:**

- Consumes: completed item, space, event, and profile pages.
- Produces: consistent blue-white immersive ArkUI visual style with stable navigation and no obvious overlap.

Implementation guidance:

- HDS is not the default path. First inspect package files. If HDS is not already available, do not add HDS imports.
- Treat native ArkUI fallback as the expected implementation.
- Keep `#007AFF` as the primary blue accent.
- Keep route names and AppStorage refresh tokens stable.
- Avoid changing repositories, models, or database schema in this task.

Steps:

- [ ] Step 1: Inspect `oh-package.json5`, `entry/oh-package.json5`, and existing imports to confirm whether HDS/UIDesignKit is available.
- [ ] Step 2: Remove stale/commented imports and unused visual leftovers if they are safe to remove.
- [ ] Step 3: Normalize `Index.ets` top tabs and bottom nav: selected state, blur/background, heights, and touch targets.
- [ ] Step 4: Normalize page safe-area/top spacing for `Index`, routed detail pages, and `ProfilePage`.
- [ ] Step 5: Ensure lists that have floating add buttons include enough bottom padding so the FAB and bottom nav do not cover content.
- [ ] Step 6: Normalize card radius, list spacing, loading states, and empty states across Item, Space, Event, and Profile pages.
- [ ] Step 7: Inspect all main pages and detail pages for text/button overlap on phone viewport.
- [ ] Step 8: Build in DevEco/deveco-code.
- [ ] Step 9: Commit with `git commit -m "style: polish ArkUI immersive navigation"`.

Acceptance:

- No unresolved HDS imports.
- App still uses working ArkUI navigation.
- Top tabs, bottom nav, headers, cards, and FABs feel consistent.
- Floating buttons do not cover important list rows.
- Blue accent remains `#007AFF`.
- Build passes.

---

## Task 8: Event Share

**Intent:** Add a useful Harmony platform feature with lower risk before service card work.

**Files:**

- Modify: `entry/src/main/ets/pages/EventDetailPage.ets`

**Interfaces:**

- Consumes: existing `event`, `checklist`, `prepared`, and `total` state in `EventDetailPage`.
- Produces: shareable plain-text checklist for an event.

Implementation guidance:

- Reuse the current loaded EventDetailPage state.
- Do not create a new repository layer for sharing.
- Do not duplicate event/checklist loading unless the page state is not available.
- Prefer the standard HarmonyOS share/Want API supported by API 24 in the local DevEco environment.
- If the exact API cannot be proven build-safe, add a graceful fallback and report it. The page must not crash.

Steps:

- [ ] Step 1: Add a small share action to `EventDetailPage` without disturbing the existing back/complete actions.
- [ ] Step 2: Add a helper that formats plain text:
  - event title
  - date if present
  - status/progress
  - note if present
  - checklist lines with prepared/unprepared markers
- [ ] Step 3: Use a build-supported HarmonyOS share/Want API.
- [ ] Step 4: If the API is unavailable, keep a non-crashing fallback and document the limitation in the task report.
- [ ] Step 5: Build and manually test active event, completed event, and empty-checklist event if possible.
- [ ] Step 6: Commit with `git commit -m "feat: add event checklist sharing"`.

Acceptance:

- Share text contains actual event and item data.
- Sharing failure or unsupported API does not crash the page.
- Existing checklist toggle and complete behavior still works.
- Build passes.

---

## Task 9: Harmony Service Card

**Intent:** Add a service card with controlled platform risk. Build-safe template first, live data second.

**Files:**

- Create/Modify form ability and resources according to DevEco generated API 24 Form template.
- Modify: `entry/src/main/module.json5`

**Interfaces:**

- Consumes: active event data if accessible from the form context.
- Produces: desktop card showing active event/progress, or a graceful static/empty state if live data is not safe.

Implementation guidance:

- Do this in two stages.
- Stage 1 must produce a minimal build-safe card registered in `module.json5`.
- Stage 2 may connect to event data only if RelationalStore/repository access is safe in the form context.
- Do not break `EntryAbility` or `EntryBackupAbility`.
- Do not commit generated local state.

Steps:

- [ ] Step 1: Use DevEco's Harmony Form template appropriate for HarmonyOS 6.1 / API 24.
- [ ] Step 2: Add the generated form ability/resource/profile files with minimal edits.
- [ ] Step 3: Register the form in `entry/src/main/module.json5`.
- [ ] Step 4: Render a build-safe empty/static card state.
- [ ] Step 5: Build the app. If the template/registration fails, fix this before attempting live data.
- [ ] Step 6: If build-safe, connect the card to the first active event and checklist progress using existing repository APIs.
- [ ] Step 7: If repository access is unsafe in the form context, keep the static/empty card and document the limitation.
- [ ] Step 8: Test card picker or DevEco form preview if available.
- [ ] Step 9: Commit with `git commit -m "feat: add active event service card"`.

Acceptance:

- App still builds.
- Service card appears in launcher card picker or DevEco form preview when that environment supports testing it.
- Empty state is meaningful.
- Live event progress appears only if implemented in a build-safe way.
- `module.json5` remains valid.

---

## Task 10: Competition Readiness Materials

**Intent:** Prepare reviewer-facing materials that match the actual implemented app.

**Files:**

- Create: `docs/competition/architecture.md`
- Create: `docs/competition/test-report.md`
- Create: `docs/competition/screenshots.md`

**Interfaces:**

- Consumes: completed app after Tasks 7-9.
- Produces: project explanation, verification report, and screenshot index.

Steps:

- [ ] Step 1: Document architecture: `EntryAbility`, database init, pages/components/data/model layers, RelationalStore tables, and route registration.
- [ ] Step 2: Document user flows: item create/search/detail, space drill-down, event checklist, profile dashboard.
- [ ] Step 3: Document Harmony features: ArkUI immersive polish, share, service card status.
- [ ] Step 4: Capture screenshots for item, space, event, profile, share, and service card if possible.
- [ ] Step 5: Create `screenshots.md` with actual screenshot references only. If screenshots cannot be captured, write a pending checklist instead of inventing files.
- [ ] Step 6: Write `test-report.md` with build result, test date, device/emulator if available, pass/fail matrix, and known issues.
- [ ] Step 7: Commit with `git commit -m "docs: add competition delivery materials"`.

Acceptance:

- A reviewer can understand the app without opening code first.
- Test report matches real build/manual test results.
- Screenshot document references only real files or clearly marks pending screenshots.

---

## Task 11: Final Cleanup And Review Handoff

**Intent:** Hand the work back to Codex or a human reviewer with a clean, verifiable state.

Steps:

- [ ] Step 1: Run `git status --short`.
- [ ] Step 2: Confirm `.hvigor/`, `.idea/`, `oh_modules/`, `build/`, and generated output are not staged or committed.
- [ ] Step 3: Run full DevEco/deveco-code build.
- [ ] Step 4: Exercise item create/search/detail.
- [ ] Step 5: Exercise space create/drill-down/item-by-space.
- [ ] Step 6: Exercise event create/link/checklist/complete/share.
- [ ] Step 7: Exercise profile stats.
- [ ] Step 8: Inspect UI for overlap on phone viewport.
- [ ] Step 9: Verify service card picker/preview if available.
- [ ] Step 10: Verify docs and screenshot references.
- [ ] Step 11: Report latest commit hash, build result, manual test result, and known issues.

Acceptance:

- Working tree contains only intentional source/config/docs changes.
- Build result is known and reported exactly.
- Manual flow status is documented.
- Remaining known issues are explicit.

---

## Review Gate For Codex

After `deveco-code` finishes each remaining task or batch:

- Check `git status --short`.
- Reject commits that include `.hvigor/`, `.idea/`, `oh_modules/`, `build/`, or generated build output.
- Review `main_pages.json` for routable pages.
- Review `module.json5` after service card changes.
- Review ArkTS imports for unresolved HDS, wrong kit imports, or stale imports.
- Review event share behavior for non-crashing fallback.
- Build in DevEco/deveco-code.
- Manually exercise the flow implemented by that task.
- Commit only after build and manual flow pass, unless reporting a blocked build with exact errors.

Final review before merging:

- Run full app build.
- Exercise item create/search/detail.
- Exercise space create/drill-down/item-by-space.
- Exercise event create/link/checklist/complete/share.
- Exercise profile stats.
- Inspect UI for overlap on phone viewport.
- Verify service card behavior or clearly documented fallback.
- Verify competition docs and screenshots.

## GSTACK REVIEW REPORT

Plan status: optimized continuation plan for Task 7 onward.

Verdict:

- Keep the final goal unchanged.
- Do not restart completed Tasks 1-6.
- Treat ArkUI fallback as the default visual path because HDS availability is unproven in current project dependencies.
- Move event share before service card to land a lower-risk Harmony feature first.
- Split service card into build-safe template first and live-data binding second.

Risks:

- HDS availability is unproven. Native ArkUI polish is the safe default.
- Service card API/template details may depend on the local DevEco environment. Use generated template output as source of truth.
- Command-line `hvigor` may not be available in Codex shell, so build verification may need DevEco/deveco-code.
- Local generated state such as `.hvigor`, `.idea`, `oh_modules`, and `build` must stay out of source commits.

NO UNRESOLVED DECISIONS
